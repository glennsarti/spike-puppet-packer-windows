- Required Modules -
puppetlabs-powershell

All modules installed with the following command-line:

puppet module install <MODULE> --target_dir C:/Windows/Temp/puppet/modules

- Running Puppet Code -
The command to kick off the puppet run:

puppet apply --modulepath c:\windows\temp\puppet\modules c:\windows\temp\puppet\manifests\site.pp
